[global]
        map to guest = Bad User
        log file = /var/log/samba/%m
        log level = 10

[backups]
        path = /backups/
        read only = no
        guest ok = yes

[backups]
        path = /share/
        read only = no
        guest ok = yes